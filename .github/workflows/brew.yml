name: Brew install

on: [push]

jobs:
  build:
    strategy:
      matrix:
        os: [macos-latest]

    runs-on: ${{matrix.os}}

    steps:
    - uses: actions/checkout@v2

    - name: Install
      run: |
        brew update
        brew tap bbc/audiowaveform
        brew install audiowaveform
